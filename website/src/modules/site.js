const initialState = {
  title: 'Whose Woods',
  api: 'http://127.0.0.1:3001',
  loggedInEndpoint: '/workspace',
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
